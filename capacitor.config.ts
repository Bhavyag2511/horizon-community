import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.horizon.community",
  appName: "Horizon Community",
  webDir: "dist",
};

export default config;